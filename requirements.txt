# Core ranking + API. Pinned to widely-available, compatible versions.
fastapi==0.111.0
uvicorn[standard]==0.30.1
numpy>=1.26,<3.0
scipy>=1.11
scikit-learn>=1.4
pandas>=2.0

# OPTIONAL — deeper semantic embeddings (sentence-transformers).
# The ranker auto-detects this and falls back to TF-IDF + LSA if absent, so the
# repo runs end-to-end without it. To enable: install the two lines below and
# run with the env var RANKER_USE_ST=1.
# sentence-transformers>=2.7
# torch>=2.2
